# HANDOVER.md — 交付物说明 & 能力边界

> 给「下一个接手这个 skill 的人 / 下一次醒来的自己」看。先读这个，再动文件。

---

## 一、目录结构与各文件职责

```
五行能量说明书/
├── SKILL.md                 # 唯一的工作流入口（铁律 + 话术 + 七层报告 + content.json 契约）
├── INSTALL.md               # 安装与首次使用（放哪 / 装依赖 / 自检 / 开始用）——新用户先读这个
├── PRODUCT.md               # 这个 skill 解决什么、给谁用、不做什么
├── HANDOVER.md              # 本文件：交付物说明 + 能力边界
├── CHANGELOG.md             # 版本变更记录
├── VERSION                  # 当前版本号（v1.6 新增，update.py / 发布流程的事实源）
├── update.json              # 更新源配置（v1.6 新增；latest.json URL 列表，占位符待作者填）
├── requirements.txt         # 唯一依赖：lunar-python==1.4.8（锁版本，纯 Python 免编译；v1.4 起已 vendor，此文件仅作版本记录）
├── scripts/
│   ├── paipan.py            # 排盘脚本：出生信息 → JSON（四柱/日主/五行/身强弱初判/喜用候选/大运/流年详解）
│   ├── render.py            # 渲染脚本：paipan JSON + content.json → 完整 HTML（锁死骨架）
│   ├── update.py            # 自更新脚本（v1.6 新增）：--check / --apply --yes，sha256 校验 + 更新前整体备份
│   └── vendor/lunar_python/ # vendored 依赖（v1.4）：lunar-python==1.4.8 纯 Python 440K，免 pip 即插即用
├── templates/
│   └── report_template.html # 浅色版 HTML 骨架（含 {{占位符}}、双版本切换 CSS/JS、版本角标）
├── examples/                 # 示例目录（脱敏：本 skill 会被分享，不放置任何真实命盘，示例统一用编造信息 2001-07-01 深圳）
└── references/
    ├── 红线清单.md          # 禁用词表 + 禁用句式（动手前必读）
    ├── 五行落生活对照.md    # 喜忌 → 行为/环境/行业相性 对照表
    └── 对话与文案.md        # 话术 + 总结速览四句底稿
```

---

## 二、完整工作流（从出生信息到报告）

```
0. 解释器任意 python3 即可——lunar-python 已 vendor 进 scripts/vendor/（v1.4），免装依赖。
     仅当 vendor 目录缺失时才需 pip install -r requirements.txt。
1. 开场声明（SKILL.md 一）——真诚、严谨、专业，坦荡承认「会排八字」再抬到「能量地图」
2. 收集 4 样信息（SKILL.md 二）——一次问全，不挤牙膏
3. 排盘（SKILL.md 四）
   $PY scripts/paipan.py --date <日期> --time <时间> --place <城市> --gender <性别> > paipan.json
   # 城市表查不到时：先问到经度，再 --lon <经度> 重排（东经为正，西经为负）
   · 判断已下沉：身强弱初判 / 喜用候选 / 根气 / 调候 都在 JSON 里，AI 只翻译
   · ⚠️ 看顶层「需确认事项」，不是只看「时辰边界提示」——非空必须先向用户确认，再出报告
   · 排盘前先验主体：档案/记忆里已有出生口径时先比对，对不上就先问，别自己猜（SKILL.md 四·4.0）
4. AI 写文案（content.json，契约见 SKILL.md 六）
5. 渲染（SKILL.md 六）
   $PY scripts/render.py --paipan paipan.json --content content.json --out 报告.html
   · AI 不手写整页 HTML——骨架由 render.py + 模板锁死，防止静默失效
6. 收尾 + 留追问入口（SKILL.md 七）
```

---

## 三、关键设计决策（别推翻，除非你知道为什么）

| 决策 | 结论 | 原因 |
|------|------|------|
| 排盘库 | **lunar-python==1.4.8**（不是 sxtwl），**vendor 进 scripts/vendor/**（v1.4） | 纯 Python 免编译；vendor 后任何 python3 即插即用，跨 agent 环境（Codex 等）零依赖 |
| 接近从弱口径 | `cong_weak=True` 时喜用按**顺势**取（从旺神，忌印比），不再补日主与印星（v1.4） | 修 Codex 反馈：旧版 fuyi_hint 说「不宜硬补」却仍输出补印比，前后冲突 |
| 判断下沉 | paipan.py 算「判断」，AI 只做「表达」 | 解决同一张盘结果漂移 |
| 五行计数口径 | 展示用「含藏干」为主，后缀标「明面 N」 | 本气会低估能量；双口径避免误会 |
| 时辰校正 | 默认钟表时间，不做无条件真太阳时校正 | 仅整点 ±15 分钟才出分叉提示 |
| UI | 浅色暖米白底（非深色星空风） | 已定稿 |
| 双版本 | 01–06 各写 `.v-plain`（结论）与 `.v-pro`（结论+为什么），两套独立自洽 | 小白 / 进阶 都能读 |
| 第④句措辞 | 「盘面结构」（非「能量格局 / 命格 / 主结构」） | 最客观、不武断 |
| 调候 vs 扶抑冲突 | 调候优先 | 季节因素对日主更本质 |
| 燥土 vs 湿土 | 未/戌燥土不帮身，辰/丑湿土生金 | 避免机械「缺土补土」 |

---

## 四、能力边界

- **能做的**：排盘（四柱/日主/五行/身强弱初判/喜用候选/大运/流年详解）、生成浅色 HTML 报告、白话/专业双版本、大运干支按五行上色、流年四节（事业/健康/感情/注意事项，当年+明年）。
- **不做的**：断命格、判吉凶、预测具体事件、给决定、医疗/投资建议。（详见 PRODUCT.md 与 `references/红线清单.md`）
- **已知未做（后续可加）**：紫微斗数（需精确到分钟，出生时间口径见项目记忆）。
- **已知短板（2026-09-03 盘点；流年详解已于 v1.2 补上）**：
  - 城市表只有 12 座（深/梅/广/惠/京/沪/蓉/渝/汉/杭/汕/莞）。未命中时退化到东经 120° 近似并写入「需确认事项」——**这个方案依赖 AI 主动读该字段才不出错**。彻底解决要换带经纬度的城市库。
  - **「对象盘 / 两人能量互补」已撤掉**（2026-09-03 改写）：收尾话术已改为「帮朋友/家人也排一份」（中性、可实现），原「对比两个人的能量互补」是空头支票——全库无合盘逻辑，且天然踩「合不合」红线。
  - 无 `@media print`——用户想把报告存成 PDF 会排版崩。
  - `examples/` 是空目录（没有标准产出的样板）。
  - `references/五行落生活对照.md` 偏薄（v1.2 已补「三层属性」一节，但 10 日主 × 喜忌的细对照仍可扩）。
  - 缺「这不就是巴纳姆效应吗」的质疑应答话术。

---

## 五、示例与真实口径（脱敏）

- 本 skill 会被分享，所有示例出生信息均为编造，统一用 `2001-07-01 凌晨 4:55 广东深圳 女`。
- 真实命盘口径不写进本 skill，见项目记忆 `.workbuddy/memory/MEMORY.md`。

---

## 六、改动的坑（血泪经验，别再踩）

1. **写 SKILL.md 前先 Read 刷新**：若先 `cp` 同步过文件、mtime 变了，直接 Write 会报「File has been modified since read」。
2. **打包 zip 前先删同名文件**：桌面已存在 0 字节同名 zip 时，`zip` 会报「Zip file structure invalid」（退出码 3）。
3. **白话版文案别用模板里没有的 class**：`.wxlist` / `.do-box` 若模板缺失会导致白话版排版塌掉（现已补进 report_template.html）。
4. **render.py 占位符名**：`{{总结速览}}`、`{{能量地图_白话}}` 等，写 content.json 时别和映射表对不上。
5. **`main()` 里循环变量别再用 `d`**：`for d in yun.getDaYun()` 会遮蔽上面 `y, mo, d` 的「出生日」，之后任何用到日期的逻辑拿到的都是 DaYun 对象（2026-09-03 加夏令时检测时炸过一次，已改用 `dy`）。
6. **别给「真太阳时偏差」设固定分钟阈值**：深圳就在 -23.8 分钟，会次次报警变成噪音。只提示「钟表时间与真太阳时**不在同一时辰**」——那才是真会改变时柱的信号。
7. **流年板块结构（v1.2）**：`paipan.py` 输出 `流年详解`（当年+明年：干支五行十神/主题/与命局六合六冲相刑/与当前大运关系/喜忌归属，事实层）；`render.py` 的 `build_liunian()` 锁死数据卡 + 四节标题（事业/健康/感情/注意事项），AI 只在 content.json `流年.白话/专业.年["2026"]` 里写正文。年份 key 是字符串四位，别写成 int（JSON 里无所谓，但 render 用 `str(xq["年"])` 取值）。
8. **板块编号已变（v1.2）**：05=流年「今年，风往哪吹」、06=决策镜、07=边界声明。白话/专业双版本覆盖 01–06；写 SKILL 改动时别再引用旧的「05 决策镜 / 06 边界」。
9. **从弱口径（v1.4）**：`judge_strength` 输出 `cong_weak`（身弱+无根+yin_help≤1.0）。为 true 时 `guess_xiyong` 按顺势取（喜=最旺五行+其所生，忌=日主+印星）；**旺神所生若恰是印/比劫，归忌不归喜**（从格忌印比优先，防同一五行既喜既忌）。该分支极罕见（藏干里几乎必有印比），用合成盘单测覆盖，不要拿真盘瞎试。
10. **最终 HTML 不放 /tmp（v1.5）**：报告输出到系统临时目录会导致预览「无法加载该文件」。`--out` 只指向用户可访问目录（工作区/桌面/用户指定）；中间产物（paipan.json、content.json）才可放临时目录。
11. **对话回复无自动红线检查（v1.5）**：grep 四项检查只覆盖渲染出的 HTML，聊天文字查不到。实测追问回复滑出「注定（此处删）」（红线词+草稿残留）。防线写在 SKILL.md 7.1：对话回复发出前默查禁用词 + 通读成稿。
10. **vendor 目录别删**（v1.4）：`scripts/vendor/lunar_python/` 是免依赖的关键。升级 lunar-python 版本时整个目录替换 + 改 requirements.txt + 全回归（节气算法差一个版本结果可能不同）。
