---
name: 五行能量说明书
description: 输入出生信息（阳历日期/时间/出生地/性别），输出一份「五行能量说明书」——不断命格、不判吉凶，只讲能量结构、喜忌与顺势而为。触发词：五行能量、能量说明书、八字、命盘、算五行、排盘、看八字、喜用神、大运。
---

# 五行能量说明书

> **一句话定位**：任何人输入出生信息，得到一份「能量地图 + 使用说明」——是**自我认知的镜子**，不是**预测命运的剧本**。

---

## 前置、判断标准：什么时候才真的去排盘（防误触发）

> 这一节**先于一切**读。看到任何「用户提到 skill、给出信息、让你做事」的输入，先按这张表判断，再决定动不动。

| 用户输入形态 | 应该怎么做 |
|---|---|
| 给 4 项出生信息（前文已明确排盘意图） | 走完整流程（开场 → 收信息 → 排盘 → 渲染 → 收尾） |
| 给 4 项出生信息，但前文**没**明确排盘意图 | 先一句话确认：「这是给你排、帮朋友排，还是想体验一下看一眼？」——否则容易把 demo / 试玩当真盘处理 |
| 拿 skill 投喂：「这个怎么用」「教我怎么用」「你能做什么」 | **只语言回答工作流**，不排盘、不渲染、不写任何文件（标准应答见 `references/对话与文案.md` 4.1） |
| 问「怎么安装 / 怎么开始用」 | 指向 `INSTALL.md`：放 skills 目录 → 跑自检命令（依赖已 vendor 内置，免安装）→ 对话里给 4 项信息。**只语言指引，不替用户装**（除非用户明确让你动手） |
| 演示 / 教学 / 教别的 AI / 借 skill 评估 | **同上**——语言回答，不出报告 |
| "让我试试" / "随便生成一个看看" | 默认按 demo 处理，**必须**先说一句"下面排的是虚拟样本，与你本人无关" |
| 「检查更新 / 有没有新版本 / 升级 skill」 | 走「九、检查更新」流程：跑 `scripts/update.py --check` → 有新版先展示 changelog 并征得同意 → 才准 `--apply --yes`。**绝不静默更新** |

**反面教材**：另一个 AI 把本 skill 丢给 WorkBuddy 说「怎么用」，WorkBuddy 看到 4 样信息直接走流程、按一套「正经」话术虚拟时间生成了一份"正经"风格的报告——这份报告没人想要、没人会用、还消耗了资源。本节就是为了挡住这类误触发。

---

## 零、先立三条铁律（做任何事之前先读）

1. **不断命格** — 不说「你是X命」「命里带Y」「你会Z」。
2. **不判吉凶** — 不说「这年倒霉/发财」「晚年有福/有灾」。
3. **不预测具体事件** — 不说「你会遇贵人/离婚/破财/升职」。

**完整红线（禁用词表 + 禁用句式）见 `references/红线清单.md`，动手前必须读一遍。**

---

## 一、开场声明（对话话术）

> 用户说「帮我看看命 / 五行 / 能量」时的第一句。语气要**真诚、严谨、专业**——不端着否定「算命」，也不滑向玄学腔；坦荡承认「会排八字」，再把它抬到「能量地图」的高度。

```
你好，我是「五行能量说明书」。

我能根据你的出生信息排出八字，但不止于告诉你"你是什么命"——
我会帮你画一张「五行能量地图」，讲清三件事：
· 你天生带着哪几种五行能量，各是旺还是弱
· 什么在滋养你，什么在消耗你
· 这些能量在你人生的不同阶段会怎么流动，该怎么顺势

它是认识自己的一面镜子，不是写好的剧本：
帮你看清"为什么会有这些倾向"，而不是"注定会发生什么"。

你的出生信息只用在这一份分析里，留在本地和当前会话，不外传。
过程中有任何不清楚，随时问我。
```

---

## 二、收集信息（输入模板 + 话术）

开场之后紧接着问，**一次问全，不挤牙膏**：

```
开始之前，我需要 4 样信息（越精确越好）：

1. 出生日期（阳历，就是身份证上的公历日期）
2. 出生时间（几点几分，最好能到分钟）
3. 出生地（哪个城市，用来校正时间）
4. 性别

可以直接这样发我，比如：
「2001年7月1日 凌晨4点55分 广东深圳 女」

时间只记得大概也没关系（比如「半夜」「天刚亮」），
给我个范围就行，我会提醒你可能的影响。
```

**输入模板（内部）**：

```
出生日期：阳历 YYYY-MM-DD
出生时间：钟表时间 HH:MM（尽量精确到分钟）
出生地：城市（用于真太阳时校正）
性别：男 / 女
```

---

## 三、引导话术（5 个边界场景）

### 场景 1：用户只说一句「帮我看看五行」

→ 用上面「收集信息」的话术一次问全，不逐项挤牙膏。

### 场景 2：时间记不清

```
你只记得生日、不记得具体几点的话，我只能排出「年月日」三柱，
时柱会空着——命盘会少一块，但五行能量的大致结构还是能看的。
先把知道的给我，缺的部分我们再看怎么办。
```

### 场景 3：时辰卡在整点（比如凌晨 3 点）

```
你的出生时间刚好卡在「丑时」和「寅时」的交界，
这两个时辰排出来的盘差别很大，我不能替你猜。
你更确定是 3 点之前，还是 3 点之后？
家里要是有准确的出生记录，也建议再核对一下。
```

### 场景 4：出生地不在城市表

（脚本会在 `需确认事项` 里输出「出生地经度未知」。城市表目前只有 12 座：
深圳 / 梅州 / 广州 / 惠州 / 北京 / 上海 / 成都 / 重庆 / 武汉 / 杭州 / 汕头 / 东莞，
之外的城市一律走这个场景。）

```
这个城市我暂时查不到经度，能告诉我它大概在哪个省、什么位置吗？
知道经度最好，我直接按经度排。

这一步不是走形式——差得远影响很大。比如新疆和北京，
钟表时间都是中午 12 点，但按当地实际的太阳位置算差了两个多小时，
时柱可能整个不一样。
```

拿到经度后用 `--lon <经度>` 重排（东经为正，西经为负）。

### 场景 5：出生在 1986–1991 年（中国夏令时期间）

（脚本会在 `需确认事项` 里输出「夏令时」。这六年每年 4/5 月至 9 月，
中国把钟表时间统一拨快了 1 小时。）

```
你的出生日期正好落在中国实行夏令时的那几年。
那时候的钟表时间比标准时间快 1 小时。

想跟你确认一下：出生记录上写的那个时间，
是当时的钟表时间，还是后来已经换算回标准时间了？

如果是前者，排盘要用的时间得减掉 1 小时——
这一减如果跨了时辰，时柱就不一样了，所以我不能替你决定。
```

---

## 四、排盘（判断下沉，不自由发挥）

### 4.0 排盘前后：两项核验（2026-09-03 新增，跳过会出误导性报告）

**① 排盘前先验主体：这是谁的盘？**

如果项目记忆 / 命理档案里**已经存有本人的出生口径**，先拿本次给的信息跟它比对：

- 一致 → 继续排；
- 不一致 → **停下来问清楚，不要自己猜主体**。

> 真实教训：用户开口给的日期排出来是丁火日主（喜木火），档案里记的却是庚金日主（喜金水），
> 喜用几乎完全相反。主体搞错，整份报告「往哪补、往哪避」会全部反过来——那就不是参考，是误导。
> 常见情形就三种：帮别人排、记错了、拿测试盘验证 skill。问一句的成本远低于返工。

**② 排盘后先读 `需确认事项`：非空 → 必须先问，再写报告**

`paipan.py` 会把所有「必须跟用户确认」的项汇总进顶层 `需确认事项`（list），每条含 `类型 / 说明 / 提示`：

| 类型 | 触发条件 | 你要做什么 |
|---|---|---|
| `出生地经度未知` | 城市表查不到该城市（表内仅 12 座） | 问经度或所在省市，用 `--lon` 重排 |
| `真太阳时跨时辰` | 钟表时间与真太阳时**不在同一个时辰** | 讲清两个口径的差别让用户选，**不替用户选流派** |
| `夏令时` | 出生日期落在中国夏令时期间（1986–1991） | 确认出生记录上的时间是当时钟表时间，还是已换算的标准时间 |
| `时辰边界` | 钟表时间落在时辰起点 ±15 分钟内 | 请用户确认走哪个时辰 |

**只有 `需确认事项` 为空，才可以直接出报告。**
（排盘本身不卡住——JSON 照常按「钟表时间」口径输出，但报告要等确认后再给。）

---

**解释器：任意 python3 即可**（v1.4 起 `lunar-python` 已 vendor 进 `scripts/vendor/`，免装依赖、不挑环境——WorkBuddy / Codex / Claude Code / 裸终端都能直接跑）：

```bash
PY=python3
```

> 仅当 `scripts/vendor/` 目录缺失时才会报 ModuleNotFoundError，此时按 INSTALL.md 装依赖：
> `$PY -m pip install -r requirements.txt`（锁定 lunar-python==1.4.8）。

再运行 `scripts/paipan.py`：

```bash
# 城市表内有（深/梅/广/惠/京/沪/蓉/渝/汉/杭/汕/莞）
$PY scripts/paipan.py --date <日期> --time <时间> --place <城市> --gender <性别>

# 城市表查不到 → 先按场景 4 问到经度，再带 --lon 排（东经为正，西经为负）
$PY scripts/paipan.py --date <日期> --time <时间> --place <城市> --lon <经度> --gender <性别>
```

实测可用（编造示例盘，2026-09-03 验证通过）：

```bash
cd <skill 目录>   # 即本 SKILL.md 所在目录，如 ~/.workbuddy/skills/五行能量说明书
$PY scripts/paipan.py --date 2001-07-01 --time 04:55 --place 深圳 --gender 女
# → 辛巳 甲午 乙丑 戊寅，日主乙木（阴），含藏干 金3 木3 水1 火3 土5
# → 需确认事项：1 条「时辰边界」（04:55 落在卯时起点 ±15 分钟内）

# 城市表查不到时（如西安）会输出「出生地经度未知」；拿到经度后重排：
$PY scripts/paipan.py --date 1996-04-10 --time 12:30 --place 乌鲁木齐 --lon 87.6 --gender 女
# → 需确认事项：「真太阳时跨时辰」（12:30 属午时，真太阳时 10:20 属巳时，差 -129.6 分钟）
```

渲染同理，也用 `$PY`（render.py 仅用标准库，但统一口径不踩坑）：

```bash
$PY scripts/paipan.py --date ... --time ... --place ... --gender ... > paipan.json
$PY scripts/render.py --paipan paipan.json --content content.json --out 报告.html

`render.py` 还支持 `--anon`（**安全分享用**）：
报告生成后如果想发给别人参考，加 `--anon` 后页面头部的「阳历日期 · 性别 · 出生地（真太阳时）」会被替换成「已隐去」，仅保留八字作为报告主体（其他字段 HTML 内已移除）。**日常自留用不要带 --anon**，脱敏后看不到出生信息不便后续追问。
```

**关键原则：排盘脚本已经算出「身强弱初判 + 喜用候选 + 根气 + 调候」，这些是「判断」，不是「表达」。** 你的任务是：

- 只把脚本输出的**判断结果「翻译成人话」**，不在判断层面自由发挥；
- 若脚本「需确认事项」非空（时辰边界 / 出生地经度未知 / 真太阳时跨时辰 / 夏令时，任一）→ **必须先请用户确认**，不自行决定；
- 若脚本「身强弱」是「中和」→ 结合根气与透干做**有限的**细化判断，但仍以脚本数据为准，不另起炉灶。

**排盘口径（已内置，无需你处理）**：

- 时柱默认按**钟表时间**排，不做无条件真太阳时校正；真太阳时只作参考输出。
- 仅当钟表时间落在时辰边界（整点 ±15 分钟）时，脚本才输出「分叉提示」，此时你要请用户确认走哪个时辰。
- `五行计数` 输出两个口径：`本气`（4 天干 + 4 地支本气，8 字）与 `含藏干`（4 天干 + 4 地支全部藏干）。**展示时以「含藏干」为主口径**（与身强弱判断一致，避免低估能量），「本气」作参考对比。

---

## 五、七层报告（固定骨架，填充而非创作）

> **双版本铁律**：01–06 每个分析板块，都要写**两套各自独立、完整自洽**的内容，用「白话 / 专业」按钮全局切换。
> - **白话版（`.v-plain`）＝ 纯结论，零解释**。只给小白最关心的四件事：①身强身弱（一句话结论）；②五行缺什么、多什么；③**适合做什么**；④**不适合做什么**。凡是「为什么」——十神、生克、扶抑、调候、无根、燥土、庚辛对比、大运合冲刑破——**一律不进白话版**。宁可少讲，不可多解释。让完全不懂玄学的人也能秒懂、能照着做。
> - **专业版（`.v-pro`）＝ 结论 + 为什么**。在结论之上补原理：日主阴阳大小（庚辛/甲乙…对比）、十神怎么定、身强弱怎么算（根气/燥土/扶抑）、喜用三层怎么取（扶抑/调候/通关）、大运干支与原局的合冲刑破。术语可直给，不必再配人话。
> - 两版**独立可读**：专业版也要带结论（不假设读者先看过白话版）；白话版不能残留「为什么」或任何术语。
> - 00 盘面速览、总结速览、07 边界声明**不分版本**（客观数据与边界是公共底座）。

### 00 盘面速览（数据层 · 只陈述不评价）

四柱、日主、五行计数、十神分布。**像体检报告的「指标数值」，不加一句评价。** 这是客观锚点，也是反巴纳姆的第一道防线。

- 五行条以「含藏干」为主口径展示（能量全貌），每行末尾用小字标「明面 N」作对比（藏干暗能量 vs 明面 8 字），**不评价多寡好坏，只陈述数值**。
- 五行条顶部 legend 文案必须用白话、让完全不懂玄学的人也能秒懂：
  > 「大数＝这股能量的真实总量（包括藏着的）· 小字＝明面上能看得到的」
  > （不要写「条长＝含藏干」「括弧＝本气」这类术语，绝大多数普通人看不懂。）

### 总结速览（00 正下方 · 板块标题用「先看这里」）

> 固定四句白话，让人不用往下滚就知道关键信息。**每句有固定模板，AI 只填变量，不自由发挥。**

**第①句：定性**

- 模板：你是 **{{日主五行}}**——{{日主五行}} 的一种，{{一句白话描述日主阴阳性格}}。
- 例子（庚金）：你是**庚金**——金的一种，硬、直、有原则、扛得住事。
- 通用对照：
  - 庚金：硬、直、有原则、扛得住事
  - 辛金：精致、讲究、敏锐、重修饰
  - 甲木：直、向上、敢闯、不服输
  - 乙木：柔韧、灵活、随和、适应力强
  - 丙火：开朗、外放、有热度、照亮周围
  - 丁火：温暖、细腻、聚焦、有洞察
  - 戊土：稳重、厚实、包容、扛大局
  - 己土：温润、细致、滋养、注重关系
  - 壬水：大气、灵活、善于流动、有格局
  - 癸水：细腻、敏感、内敛、善于渗透

**第②句：身强弱**

- 模板：你整体**{{身强弱等级}}**，{{一句白话解释「需要补什么 / 已经是好的」}}。
- 例子：你整体**偏身弱**，天生需要「补水润燥、补金帮身」来扶一把。

**第③句：喜用忌神**

- 模板：最滋养你的是 **{{喜用1}}** 和 **{{喜用2}}**；最消耗你的是 **{{忌1}}**（{{白话场景}}）和 **{{忌2}}**（{{白话场景}}）。
- 例子：最滋养你的是**水**和**金**；最消耗你的是**火**（压力焦虑）和**木**（机会贪多）。

**第④句：盘面最突出的一点（定稿口径：用「盘面结构」）**

- 模板：你的**盘面结构**很清楚：{{客观描述能量分布}}——这是你能量分布的基本面貌。
- 例子（示例盘）：你的**盘面结构**很清楚：火与土占据主位，金与水偏弱——这是你能量分布的基本面貌。
- ⚠️ **只许用「盘面结构」这个客观措辞**，禁用「能量格局」「命格」「主结构」等带判断意味的说法；描述只能用能量分布事实，不能加「命不好」「注定苦」这类结论。

### 01 五行能量地图（解读层 · 翻译 + 落生活）

- **第一节必是「你是什么人」定性句（铁律）**：白话版第一句话必须把读者定性——例如：「你是庚金——金的一种，硬、直、有原则、扛得住事。」这句话承担「看完盘面数据后第一句定性」的入口作用，让读者立刻知道「我身上这股能量是什么」。**这句话只能放在 01，不能挪到 00 盘面速览**（00 保持纯数据、不评价）。
- **「日主的大小」（阴阳）是专业内容，放 `.v-pro`**：同一个五行，阴阳是两种脾气。庚金是阳金（刀剑矿石，硬直刚健），辛金是阴金（珠玉首饰，精致重修饰）；甲木阳木（参天大树）、乙木阴木（藤蔓花草）；丙火阳火（太阳）、丁火阴火（烛火）；戊土阳土（高山）、己土阴土（田园）；壬水阳水（江河）、癸水阴水（雨露）。**专业版点明「你是 X 金 / X 木」这一种，并对比另一种。**
- **白话版只给一句结论**：「你是 X 金（阳金）——像刀剑，硬、直、有原则」这样的短句，再给「缺什么 / 多什么」的清单（缺→容易怎样、多→容易怎样），**不解释为什么缺会憋、为什么多会急**。
- 讲五行的**旺弱、燥湿、结构**，不讲命运。
- **守「看功能面不看字面」**：同一个火，落在厚土是埋、落在荒原是烧——问「这能量对你起了什么作用」，不问「这是什么东西」。（此条是内部判断原则，白话版不把「埋/烧」这类比喻写出来。）
- **每讲一个专业词，配一句人话**（例：「食神」＝你表达、创造、放松的那股劲）。（白话版尽量不出现专业词；出现必配人话。）
- **每个能量结论，落一个生活场景**（例：火旺 → 你可能容易急、容易上火，注意节奏）。

### 02 什么滋养你、什么消耗你（解读层 · 身强弱 + 喜用落生活）

- **第一步必须明说「身强弱」**：用脚本的 `身强弱初判`，直接给结论（身强 / 身弱 / 中和），并解释——「身弱」≠「你这个人弱」，而是「帮你的力量少于消耗你的力量」。给出依据（根气多少、印比强弱、score），用一句生活比喻讲透（例：身弱无根的金 = 一块好钢一直放在炉子里烤）。
- 第二步讲喜用，用「**滋养你 / 消耗你**」替代「克你 / 害你」。
- **喜用/忌神必须「落生活」**（最多人关注的部分，务必做扎实）：每个喜用五行给「行为 + 环境/作息 + 行业相性」，每个忌神给「容易滑向什么 + 一个替代表达」。**对照 `references/五行落生活对照.md`，不得空谈五行名词。**
- **行业方向只讲「相性」**：用「相性偏合 / 能量上更顺」，禁止「你该转行 / 你适合做 XX」，禁止给投资建议。
- **行业属性必须讲「三层不绝对」**：行业有属性，但同一行业里不同岗位、不同工作模式属性完全不同，个人每天实际做的事又是另一层——**行业相性只是最粗的第一层，真正要调的是「每天做的事」的属性**。讲行业相性时必须带上这句纠正，别让用户理解成「命里该做 XX 行业」。口径见 `references/五行落生活对照.md` 第三节。
- 必守口径：**「身弱不是答案，能不能扶起来才是。」** 先讲脚本的「扶不扶得动」提示，再谈补不补。
- **辩证看「土」**：若身弱喜印但盘里是燥土（未/戌），要说明「燥土生金力弱、反助火，不算药」，别机械输出「缺土补土」。

### 03 同样的能量，不同的活法（解读层 · 魂 · 站 C 位）

- 这是整个报告的题眼，务必用最口语的话讲出来（**别再叫「能量的容器」，也别用「容器」这个词，对小白太玄**）：

> 「同样的能量，不同的人，过出不同的日子。」

- 同样的能量，有人舒服、有人焦躁，不是因为命好命坏，是这个人怎么接、怎么用。这一层把报告基调从「被预言」翻成「有主动权」，但**用大白话讲，不甩术语**。

### 04 往后这些年，怎么走（解读层 · 大运详解 + 顺势三步）

- **只用「气候」替代「吉凶」**：大运＝十年气候（可讲），流年＝年度气候（可参考，详见 05 节），流月流日＝**明确不预测**。
- **大运要做「详解」**，不能只给一句标签：
  1. 每个大运标出「天干五行 / 十神」和「地支五行」及其对日主的作用（帮身 / 泄 / 克 / 生）；
  2. 当前 + 未来几步大运逐条给「这十年气候 + 顺势怎么走 + 注意什么」，过去的大运可简略（弱化显示）；
  3. **大运干支按五行上色**（天干、地支分别用对应五行色 `wx-金/木/水/火/土`），别统一用主题色。
- **顺势三步（这是报告最有用的部分，务必做扎实）**：
  1. **往哪引** — 这段能量下，主动做什么能把它引到对你有用的方向；
  2. **往哪避** — 少做什么能避开它消耗你的一面；
  3. **别滑向哪** — 放任不管时容易滑向的「倾向」（用「更倾向/更容易」，不用「必然」）。
- 句式模板：「这半年 X 能量偏旺，倾向是……如果你主动做 A/B/C，就能顺势；如果不加注意，可能滑向 D。」

### 05 今年，风往哪吹（解读层 · 流年详解：事业 / 健康 / 感情 / 注意事项）

> 用户最想问的就是「今年怎么样」。这一节把当年 + 明年的流年展开成四个生活维度。
> **事实层全部由脚本算好**（`流年详解`：干支五行十神、主题、与命局的六合/六冲/相刑、与当前大运的岁运关系、喜忌归属），你只做翻译，不改判断。

- **数据卡由 render.py 自动生成**（干支按五行上色、十神标签、合冲刑关系、喜忌标签），你只写 `开头` + 每年的四节正文。
- **四节固定为：事业 / 健康 / 感情 / 注意事项**（标题由 render.py 锁死，content.json 按这四个 key 写正文）：
  - **事业**：结合流年十神主题（官杀=责任与推力、财=资源机会、印=学习休整、食伤=表达输出、比劫=合作竞争）讲「这年职场能量的风向」+ 顺势动作。**只讲气候与动作，不讲「会升职/会被裁」**。
  - **健康**：只讲**能量层面**的倾向——哪股能量过旺易耗（如火旺年易急易上火、注意睡眠节奏），给作息/节奏建议。**禁任何医疗判断、禁「会生病/有灾」**。
  - **感情**：女命看官杀、男命看财星是否为流年主题，结合流年地支与日支（夫妻宫）的合冲关系讲「关系能量的流动倾向」。**只讲倾向与相处建议，禁「会分手/遇正缘」**。
  - **注意事项**：把这一年最值得留意的 1–3 个点收成清单（来自合冲刑关系 + 喜忌归属），用「更倾向/更容易」的措辞。
- **明年可以比今年简略**（一两句一节即可），但四节都要有。
- 白话版：只讲「今年的风向 + 怎么做 + 留意什么」，不出现十神/合冲刑术语；专业版：把 `流年详解` 里的十神、合冲刑、喜忌归属讲透。
- 守红线：流年只到「年度气候可参考」，**不预测具体事件、不判吉凶**；预测力排序（大运 > 流年 > 流月流日）见第八节哲学锚点。

### 06 做选择时，只问一句（解读层 · 收在问题，不收在结论）

- 报告不收在「告诉你结论」，收在「给你几个问题」。
- 犹豫时自问：「**这个选择在靠近我的喜用，还是靠近我的忌？**」
- 指南针不是剧本——指方向，不指路径。

### 07 边界声明（收口层 · 朴素，不甩心法）

- 明确「这是镜子不是剧本，是倾向不是结果」；
- 主动提醒「感到被理解 ≠ 被预言」（反巴纳姆）；
- **结尾朴素收口，不直白甩「你就是你的用神 / 内在权威 > 外部框架」这类最高境界**（很多人没这个境界，直白说教反而隔人）。落成一句实在话即可，例：「这页纸能给你的，是方向感，不是决定权——怎么走，是你自己的事。」

---

## 六、输出（HTML 可视化）

按 `templates/report_template.html` 骨架渲染 HTML，**浅色暖米白底**（米白背景 + 白色卡片 + 深色文字，主题色随日主五行切换，**不是深色星空风**）。

**渲染方式（首选 render.py，稳定不失效）**：

- 用 `scripts/render.py`：`paipan.py` 输出 JSON → AI 只写纯文案（`content.json`）→ `render.py` 锁死骨架生成 HTML。**AI 不手写整页 HTML**（手写容易静默失效），只写文案内容。
- 用法见 `scripts/render.py` 顶部注释；文案契约见下一节「content.json 文案契约」。

**报告输出位置（2026-09-05 新用户实测踩坑，必守）**：

- `--out` **禁止指向系统临时目录**（`/tmp`、`%TEMP%` 等）——预览面板和用户都会打不开（实测：报告生成成功但预览显示「无法加载该文件」）。
- 输出到**用户可访问的目录**：当前工作区、桌面，或用户指定的位置。文件名建议带对象标识（如 `五行能量说明书_1995广州男.html`）。
- 中间产物（paipan.json、content.json、build 脚本）可以放临时目录，但**最终 HTML 不行**。

**双版本切换实现**（`templates/report_template.html` 已内置）：

- 00 盘面速览之后，放一个**非 sticky** 的切换条 `.mode-switch`（**不要用 sticky，会遮挡 01–06 每个 h2 标题**），内含一行提示「👇 这份报告有 2 种写法，挑一种看」+ 两个按钮：
  - **白话易懂版**（`data-mode="plain"`，默认激活）
  - **专业分析版**（`data-mode="pro"`，激活时实底主题色填充 + 白色文字 — 一眼看出当前版本）
  - 切换条下方加一行 tip：`↗ 往下看每个板块右上角的角标，点一下也能随时切换`
- 每个板块内用 `.v-plain` / `.v-pro` 两个容器；**01–06 每张卡片右上角放版本角标 `.vc-corner`**（plain→「简单说明」/ pro→「专业分析」，跟当前模式同步，点角标也能切换）。
- `<body data-mode="plain">`（默认白话）；CSS 已写好 `.v-pro{display:none}` 与 `body[data-mode="pro"] .v-plain{display:none}`；JS 点击切换 `data-mode` 并写入 localStorage 记忆。
- 专业版可用两种小样式增强可读性：`.why-tag`（「为什么」角标，放在段落前）与 `.rule`（原理条，放十神拆解 / 取用三层 / 病药说这类结构化原理）。

**content.json 文案契约（render.py 输入）**：

⚠️ **key 必须用下面这套中文名**（不是 "01"/"02" 数字）——render.py 的 mapping 就是按这些名字做 `{{key}}` 替换，写错会静默漏填：

```json
{
  "总结速览": {
    "定性": "你是<strong>壬水</strong>——水的一种，大气、灵活、善于流动、有格局。",
    "身强弱": "你整体<strong>偏身弱</strong>，……",
    "喜用忌神": "最滋养你的是<strong>水</strong>和<strong>金</strong>；最消耗你的是<strong>土</strong>（……）和<strong>火</strong>（……）。",
    "盘面结构": "你的<strong>盘面结构</strong>很清楚：……"
  },
  "能量地图": {"白话": "...", "专业": "..."},
  "身强弱":   {"白话": "...", "专业": "..."},
  "喜忌":     {"白话": "...", "专业": "..."},
  "容器":     {"白话": "...", "专业": "..."},
  "节律": {
    "白话": {"开头": "...", "大运解读": {"己未": "...", "庚申": "..."}, "结尾": "..."},
    "专业": {"开头": "...", "大运解读": {"己未": "...", "庚申": "..."}, "结尾": "..."}
  },
  "流年": {
    "白话": {"开头": "...", "年": {"2026": {"事业": "...", "健康": "...", "感情": "...", "注意事项": "..."}, "2027": {"...": "..."}}},
    "专业": {"开头": "...", "年": {"2026": {"事业": "...", "健康": "...", "感情": "...", "注意事项": "..."}, "2027": {"...": "..."}}}
  },
  "决策镜":   {"白话": "...", "专业": "..."},
  "边界":     "..."
}
```

- **「节律」结构特殊**：大运卡片由 render.py 自动生成（干支按五行上色、当前步高亮、过去的弱化），AI 只写每步的**解读文字**，放进 `大运解读` 的 dict，**key 用大运干支两字**（如 `"辛酉"`），8 步都要写。`开头` 里放 `<div class="dayun-list">` 开场、`结尾` 里放 `</div>` + 顺势三步。
- **「流年」结构特殊**：数据卡（干支/十神标签/合冲刑/喜忌）由 render.py 自动生成，四节标题（事业/健康/感情/注意事项）也锁死；AI 只写 `开头` + 每年四节正文（`年` 的 key 用四位年份字符串，如 `"2026"`；当年 + 明年两年，与 `流年详解` 一一对应）。
- **别手写 JSON**：文案含大量 HTML 属性（双引号），手抄极易转义出错。**改用 Python 脚本构造 dict → `json.dump(..., ensure_ascii=False, indent=2)`**（写个临时 build 脚本，改文案后重跑即可，方便重渲染）。
- 文案里允许用内容级 HTML 标签（`<p>`、`<h3>`、`<strong>`、`<ul><li>`、`<div class="rule">`、`<span class="wx-jin">`、`<span class="why-tag">` 等）做强调与列表；**禁止**写页面骨架（卡片、CSS、切换条、角标由 render.py + 模板锁死）。
- `边界`（07 边界声明）是固定文案，可直接复用默认值；若该盘排过「时辰边界确认」，要在这里补一句排盘口径说明。

**渲染后必做四项检查**（防静默失效，实测有效）：

```bash
F="输出.html"
grep -c "{{" "$F"                       # ① 占位符残留，必须为 0
grep -o -E "命好|命差|注定|破财|正缘|宿命|克你|害你" "$F" | sort -u   # ② 红线禁用词，必须为空
grep -o '<body data-wx="[^"]*"' "$F"     # ③ 主题色是否随日主五行（金/木/水/火/土）
grep -c 'class="dayun' "$F"              # ④ 大运卡片数，应为 16（8 步 × 白话/专业）
```

**输出后主动自查九件事**：
1. 有没有越过红线（禁用词/禁用句式）？
2. **身强弱有没有明确给结论 + 依据**（不是只讲「身弱不是答案」就跳过判断）？
3. **喜用/忌神有没有「落生活」**（行为 + 环境 + 行业相性），而不是只报五行名词？**行业相性有没有带上「三层不绝对」的纠正**？
4. **大运干支有没有按五行上色、有没有给详解**（天干/地支各是什么能量、对日主的作用、顺势怎么走）？
5. **流年四节（事业/健康/感情/注意事项）有没有写全、当年 + 明年都有**？健康只讲能量倾向（无医疗判断）、感情只讲倾向（无「正缘/分手」断言）？
6. 小标题是不是大白话（没有「能量的容器」「能量节律」这类玄词）？结尾有没有朴素收口（没甩最高境界心法）？
7. **01–06 是否写全了白话 + 专业两套内容**？白话是否真「是什么」（无术语/术语配人话），专业是否真「为什么」（有原理、有十神生克取用推导）？
8. **按钮 + 角标是否齐全**：顶部切换条两个按钮「白话易懂版」「专业分析版」、下面有 tip 行，01–06 每张卡片右上角都有版本角标（plain→「简单说明」，pro→「专业分析」，跟当前模式同步）。
9. **五行条 legend + 后缀是否用了白话**：legend 不出现「条长/括弧/含藏干/本气」这类术语，后缀改成「明面 N」。

---

## 七、收尾 + 后续入口（报告生成后）

> 报告生成后，不要戛然而止。给追问留入口，最后落到「镜子由你看」。

```
报告好了（见附件）。这是一张「五行能量地图」。

看完如果还想深入，可以直接告诉我：
· 「喜用神怎么落到我的工作和生活里」→ 展开喜忌的落生活
· 「我最近在纠结 XX 选择，能帮我照一下吗」→ 用决策镜陪你梳理
· 「某个大运/流年我想知道更细」→ 展开那一段气候
· 「帮朋友/家人也排一份」→ 同流程走一遍（注意开头就要确认排盘对象）

或者什么都不用说，拿着它慢慢消化也行。
镜子已经给你了，怎么看由你。
```

### 7.1 追问怎么接（2026-09-05 补，原先是空白）

报告发出后用户顺着 4 个入口追问时，**总原则：一律在对话里答，不重新渲染报告**（除非用户明确说「我要一版新的报告」）；**判断沿用本次会话的 paipan JSON，不重新排盘**（除非用户给了新的出生信息）；红线与「镜子非剧本」口径在追问里同样生效。

| 入口 | 怎么接 | 数据从哪来 |
|---|---|---|
| 「喜用神怎么落到我的工作和生活」 | 对话展开：按 `references/五行落生活对照.md` 给行为/环境/作息建议，**必须带「行业属性三层不绝对」的纠正** | paipan JSON 的喜用候选 + 身强弱 |
| 「我在纠结 XX 选择，帮我照一下」 | 决策镜陪梳理：把选项逐一对照「靠近喜用还是靠近忌」，**只帮看清，不替选**（收在问题不收在结论） | 喜用候选 |
| 「某个大运/流年想更细」 | 对话展开该段：干支五行十神、与原局关系、顺势三步；流年按 05 节四维度（事业/健康/感情/注意事项）展开 | JSON 的大运/流年详解，不重排 |
| 「帮朋友/家人也排一份」 | 走完整新流程（开场可简化），**开头先确认排盘对象**（4.0 核验①），别把新信息套到原主身上 | 重新排盘 |

**追问边界的反面**：用户追问「那我今年会不会升职/分手/破财」→ 不预测具体事件，把问题翻译回能量语言（「今年官星能量的风向是……更适合主动争取的是……」），参照 05 节红线。

**对话回复同样过红线（2026-09-05 实测事故补防）**：

- 红线清单的禁用词表**对聊天文字同样生效**（不只约束报告 HTML）——渲染后的 grep 检查查不到对话内容，所以**回复发出前必须默查一遍禁用词**（重点盯「注定 / 正缘 / 破财 / 宿命 / 命好 / 命差」）。
- **发出前通读一遍成稿**：不得残留草稿/编辑痕迹（如「（此处删）」、半句重写一半的旧句）——实测事故：回复里出现「你的能量结构注定（此处删）」，红线词 + 编辑残留双重事故。
- 拿不准的词，换「更倾向 / 更容易 / 结构上偏」这类概率语言（替换句式见红线清单第三节）。

---

## 八、哲学锚点（底层世界观，务必内化）

1. 命理是镜子，不是剧本——算的是倾向，不是结果。
2. 能量的利与害取决于容器。
3. 喜用神是决策镜，是指南针不是剧本——最高境界：你不需要算用神，你就是你的用神。（**此句只作内部定位，不写进报告结尾**——对多数读者太直白、太高，落成朴素的话即可。）
4. 预测能力曲线：大运可信 > 流年可参考 > 流月流日噪音 > 具体事件不可信。
5. 叙事的连贯性 ≠ 真理性——警惕巴纳姆、确认偏误、后视偏差。
6. 看功能面，不看字面。
7. 心学交汇：知行合一、致良知；内在权威 > 外部框架。

---

## 九、检查更新（自助升级，2026-09-05 v1.6 新增）

> 触发词：「检查更新」「有没有新版本」「升级 skill」。

**流程（三步，顺序不可乱）：**

1. **检查**：`python3 scripts/update.py --check`（纯标准库，任意 python3 可跑）。
   - 输出「已是最新」→ 直接告诉用户，结束。
   - 输出「未配置更新源」→ 告诉用户：作者还没发布更新仓库，想要新版找作者要新 zip 手动覆盖即可。
2. **展示 + 征得同意**：发现新版本时，把版本号和 changelog 原样展示给用户，**问一句「要更新吗」**。用户说更，才进第 3 步。
3. **执行**：`python3 scripts/update.py --apply --yes`。脚本会自动：校验 sha256 → 把当前版本整体备份到同级目录（`五行能量说明书_备份_vX.Y_时间戳`）→ 替换新版 → 打印备份位置。完事告诉用户：「已更新到 vX.Y，旧版备份在 XX，确认没问题后可删」。

**硬规则：**

- **绝不静默更新**——不展示 changelog、没征得同意就更新，视为违规。
- 更新后**重新阅读本 SKILL.md**（工作流可能有变化）。
- skill 是用 git clone 装的（目录里有 `.git`）：脚本自动改走 `git pull --ff-only`，同样先征得同意。
- 更新失败（网络/校验不过）：脚本不会动任何文件，把报错原样转告用户即可，不要自己想办法「修」。

---

## 附：示例信息与真实口径（脱敏声明）

- 本 skill 会被分享给他人使用，**所有示例出生信息均为编造，不指向任何真实个人**。
- 统一脱敏示例：`2001-07-01 凌晨 4:55 广东深圳 女`（仅演示输入格式）。
- 真实命盘口径**只保存在项目记忆**（`.workbuddy/memory/MEMORY.md`）与私人存档中，一律**不写进本 skill**。排真实盘时去记忆里取，别在 skill 里写死任何真人信息。
