# Changelog

## v1.7 · 2026-09-06（下载体验修复：从「能下载」到「下载下来就能用」）

- **`references/` 三个中文 .md 改英文名**（`红线清单.md`→`redlines.md`、`对话与文案.md`→`dialogue.md`、`五行落生活对照.md`→`wuxing-in-life.md`）。
  仓库内所有文件名现已全部为 ASCII。原因：GitHub「Download ZIP」在部分解压方式下会**静默跳过**非 ASCII 文件名的条目且退出码仍为 0，用户会拿到一个看起来装好、实际缺红线清单的 skill——而 SKILL.md 要求动手前必读它，属于合规风险。CHANGELOG 里的旧文件名属历史记录，保留不改。
- **新增 `scripts/selfcheck.py`**（纯标准库）：四项一次查完——必备文件齐不齐（含 `references/` 三个文档）、排盘能不能跑、渲染模板完不完整、更新源配没配。任何一项失败非 0 退出。把「静默失败」变成显式报错。
- **目录名与 `name` 统一为 `wuxing-energy-report`**：SKILL.md frontmatter 的 `name` 由中文改为 `wuxing-energy-report`（`description` 里的中文触发词原样保留，不影响唤起）。Codex / Claude Code 现在开箱即用，不用再手动改名。
- **INSTALL.md 新增「零、从 GitHub 下载后」**：补上「解压 → 去掉 `-main` 后缀 → 再放 skills 目录」这条此前完全缺失的第一步。
- **修正 INSTALL.md 自检期望值**：写成数组 `"八字": ["辛巳","甲午",...]`，实际输出是字符串 `"八字": "辛巳 甲午 乙丑 戊寅"`。
- **修正 SKILL.md 渲染自检第④项**：`grep -c 'class="dayun'` 会把流年卡片一起数进去（实际 20 而非 16），照旧文档自检会误判渲染失败。现拆成 ④ 大运 16 + ④b 流年 4 两条。
- **更新 HANDOVER.md 过时描述**：`update.json` 不再是「占位符待作者填」；补充 `selfcheck.py` 与英文根目录名。
- **README 快速开始**：补解压重命名步骤、补上漏掉的 Codex。

## v1.6 · 2026-09-05（自助更新机制：update.py + VERSION + update.json）

- **新增 `VERSION`**：skill 当前版本号（update.py 与 release 流程的唯一事实源）。
- **新增 `update.json`**：更新源配置（latest.json URL 列表）。地址暂为 `<TODO_用户名>` 占位符——作者发布公开仓库（GitHub/Gitee）后替换即启用；占位符状态下 update.py 明确提示「未配置更新源」，不报错。
- **新增 `scripts/update.py`**（纯标准库，任意 python3 可跑）：
  - `--check` 对比本地 VERSION 与远端 latest.json，报版本号 + changelog；
  - `--apply --yes` 才执行更新（无 `--yes` 直接拒绝）——下载 zip → 校验 sha256 → 当前目录整体改名备份（同级，带版本号和时间戳）→ 新版就位，失败自动回滚；
  - git clone 安装（根目录有 `.git`）自动改走 `git fetch` + `git pull --ff-only`；
  - 已实测：占位符提示 / 版本对比 / 拒绝路径 / 端到端替换 + 备份 / sha256 篡改拦截。
- **SKILL.md 新增「九、检查更新」** + 防误触发表加触发行：先 `--check` → 展示 changelog 征得同意 → 才准 `--apply --yes`，绝不静默更新。
- **维护侧（不随 skill 包分发）**：发布脚本 `release.sh` 放在仓库外（作者本地发布目录，不进 git）；发布后只需把 release.sh 生成的 zip 传到 Release、把 latest.json 提交到仓库。
- 顺手修正：防误触发表「问怎么安装」一行还写着「装 lunar-python」（v1.4 vendor 后已过时），改为「依赖已 vendor 内置，免安装」。

## v1.5 · 2026-09-05（新用户全流程实测：话术产品化 + 输出位置 + 对话红线）

模拟新用户走完全流程（问能力 → 给信息 → 出报告 → 追问工作选择），抓出 3 处问题并修复：

- **4.1 标准应答产品化**（`references/对话与文案.md`）：旧版模板讲「scripts/paipan.py、render.py、模板、INSTALL.md」等实现细节——工程语言，终端用户看不懂也不需要。改为产品化口径：只讲「你能得到什么、要给我什么、信息安不安全」，结尾收在「发我 4 项信息 + 信息只存本地不外传」的隐私承诺。工程细节仅在对方（开发者/AI）明确追问实现时再给。
- **报告输出位置规则**（SKILL.md 第六节）：`--out` 禁止指向系统临时目录——实测报告生成成功但预览「无法加载该文件」。规则：最终 HTML 输出到用户可访问目录（工作区/桌面/用户指定），中间产物才可放临时目录。
- **对话回复同样过红线**（SKILL.md 7.1 + 红线清单适用范围）：实测追问回复出现「你的能量结构注定（此处删）」——红线词 + 草稿残留双重事故，且 grep 检查只覆盖 HTML、查不到对话。新增防线：红线清单明确适用一切面向用户的文字；对话回复发出前默查禁用词（重点盯「注定」）+ 通读成稿不得残留编辑痕迹。

## v1.4 · 2026-09-05（跨 agent 环境适配：vendor 内置 + 从弱口径修复）

> 触发：Codex 环境检测反馈（6 条）。其中 4 条（写死路径/静默 114°/劫财 bug/隐私）在 v1.1–v1.3 已修，本版解决剩下 2 条 + 技能名规范。

### 新能力

- **lunar-python vendor 内置**：`scripts/vendor/lunar_python/`（纯 Python 440K）+ `paipan.py` 启动时自动把 vendor 插入 `sys.path`。**任何 python3 直接可跑，免 pip install、免 venv、不挑 agent 环境**（WorkBuddy / Codex / Claude Code / 裸终端）。vendor 缺失时才回落到环境安装的 lunar-python。
- **INSTALL.md 新增「在其他 agent 环境安装」**：目录名与 SKILL.md `name` 改 `wuxing-energy-report`（kebab-case 规范），触发词保留中文不受影响。
- **追问工作流（SKILL.md 7.1）**：收尾 4 个追问入口原先没教 AI 怎么接。现定义总原则（对话里答不重渲染、判断沿用本次 paipan JSON 不重排）+ 每个入口的接法与数据来源 + 追问越界（求预测具体事件）的翻译口径。

### 修复

- **从弱口径前后冲突（Codex 第 5 条）**：旧版 `judge_strength` 提示「接近从弱、不宜硬补」，`guess_xiyong` 却仍机械输出「补日主五行+印星」。现 `judge_strength` 输出 `cong_weak` 标志；为 true 时喜用按**顺势**取——喜=命局最旺五行（+其所生），忌=日主五行+印星；**旺神所生若恰是印/比劫则归忌不归喜**（从格忌印比优先，防同一五行既喜既忌）。`综合提示` 追加从弱口径说明。已有盘例（有根）回归零影响。
- SKILL.md / HANDOVER.md / INSTALL.md 的「解释器探测、装依赖」段落全部简化为「任意 python3 即可」。

## v1.3 · 2026-09-04（新用户使用路径验收：安装 → 问怎么用 → 首次出报告）

### 新增

- **INSTALL.md**：面向收到分享包的人——放哪（user-level / project-level）、装依赖（先探测 `import lunar_python`，再选 pip 直装或 venv）、自检命令（编造示例盘）、开始用（4 项信息一次给全）、常见问题（ModuleNotFoundError / 城市表 / --anon / 改话术）。
- **SKILL.md 防误触发表新增一行**：「问怎么安装 / 怎么开始用」→ 指向 INSTALL.md，只语言指引不替装（除非用户明确要求）。

### 修复（机器专属信息，照抄必挂）

- **解释器指引改自适应探测**：SKILL.md / HANDOVER.md 原来硬编码作者本机 venv 路径，改为「先 `python3 -c "import lunar_python"` 探测，没有则按 INSTALL.md 建 venv」，本机路径降级为一行备注（仅该机有效）。
- **SKILL.md 示例 `cd` 路径改占位符** `<skill 目录>`（原为作者本机项目路径）。
- **SKILL.md 引用 `/tmp/build_content_*.py`** 改为通用说法（临时 build 脚本），不再指向作者本机临时文件。

### 一致性

- 对话与文案.md：「4 个边界场景」→ 5 个，补场景 5（夏令时），场景 4 话术与 SKILL.md 对齐；4.1 标准应答补安装指引一句；版本节补 v1.1–v1.3。
- HANDOVER.md：目录树补 INSTALL.md / CHANGELOG.md；双版本决策「01–05」→「01–06」。
- SKILL.md 切换条说明「01–05 每个 h2」→「01–06」。

## v1.2 · 2026-09-03（流年展开 + 行业属性三层说明）

### 新能力

- **流年详解（05 板块「今年，风往哪吹」）**：`paipan.py` 新增 `流年详解`（当年 + 明年），事实层含：干支五行十神、主题归类（官杀/财/印/食伤/比劫语义 + 伴侣星按性别标注）、与命局四柱的六合/六冲/相刑（含自刑）、与当前大运的岁运并临/天克地冲/天合地合、喜忌归属（扶抑喜/扶抑忌/调候喜）。
- **render.py `build_liunian()`**：数据卡（干支五行上色 + 十神/关系/喜忌标签）与四节标题（事业/健康/感情/注意事项）锁死自动生成，AI 只写四节正文。
- **模板新增 05 流年卡片**：05=流年、06=决策镜、07=边界声明（原 05/06 顺延）。白话/专业双版本覆盖 01–06。

### 口径补充

- **行业属性「三层不绝对」**：`references/五行落生活对照.md` 新增第三节——行业层（最粗）/ 岗位层（较准）/ 个人工作内容层（最准），行业相性不是转行依据，真正要调的是「每天做的事」的属性。SKILL.md 02 板块同步要求必须带这句纠正。
- SKILL.md 05 流年写作规范：事业讲气候与动作（不预测升职/裁员）、健康只讲能量倾向（禁医疗判断）、感情只讲倾向（禁正缘/分手断言）、注意事项收成清单。

### Bug 修复

- paipan.py 头部 docstring 残留旧口径「偏差 ≥20 分钟写入需确认事项」→ 改为「跨时辰才提示」。
- SKILL.md「4 个边界场景」→「5 个」（场景 5 夏令时已存在但未计数）；切换条 tip 文案与模板不一致 → 对齐模板。
- 自查清单「八件事」→「九件事」（新增流年四节检查）。

---

## v1.1 · 2026-09-03

这次改动来自「另一个 AI 拿本 skill 丢给 WorkBuddy 后误触发排盘」、发现「劫财永远不出现」、另一 AI 评估出 6 条 bug 的复合反馈。

### Bug 修复

- **劫财永远不出现**：paipan.py `shishen()` 在「五行同 + 阴阳异」这条路径无 case，统一掉到 `return "比肩"`。新增 `if dw == tw: return "比肩" if same else "劫财"` 分支。十神测试 10 对「劫财」+ 3 对反例「比肩」全过。
- **真太阳时 / 城市经度未命中静默错盘**：新增 `resolve_lng()` 与「需确认事项」顶层字段。城市命中 → 静默用；未命中 → 标记 `城市已匹配=False`、走东经 120° 近似并要求 AI 必须先问经度。
- **接不住用户给经度**：新增 `--lon` 参数（东经为正、西经为负），优先级高于 `--place`。
- **夏令时未处理（1986–1991 中国实行过）**：新增 `check_dst()` 检测，**只提示、不自动减 1 小时**——出生记录是当时钟表时间还是已换算，只有用户/出生记录知道。
- **真太阳时跨时辰未检测**：跨时辰是真正会改时柱的信号（≥20 分钟偏差会跨入相邻时辰）；偏差小于此不报警，避免深圳 -23.8 分钟级别的噪音。
- **HANDOVER 第 35、40 行的 `python3` 命令 + lunar-python 不在 system python**：统一改为 `$PY`（即 venv 那个），避免照着跑 ModuleNotFoundError。
- **SKILL.md 第四节示例硬编码本机旧路径**（形如 `/Users/<本机用户名>/Desktop/...`）：清掉，改用 `~/.workbuddy/skills/五行能量说明书`（user-level 真实安装位置）。
- **`for d in yun.getDaYun()` 遮蔽出生日的 `d`**：我加夏令时检测时踩爆，已统一改名 `dy`。

### 新能力

- **`render.py --anon`**：HTML 头部「阳历日期 · 性别 · 出生地（真太阳时）」替换为「已隐去」，用于把报告分享给他人前的隐私保护。仅改头四个字段，八字保留作为报告主体。

### 防误触发（解决「AI 拿 skill 丢给 WorkBuddy 教用法、结果自动跑排盘」）

- SKILL.md 顶部新增 **§ 前置 · 判断标准**：一张表区分 5 种用户输入形态。明确：拿 skill 来问怎么用 / 教学 / 评估 → **只语言回答工作流，不排盘、不渲染、不写文件**。
- 给 4 项信息但前文未确认排盘意图 → 先一句话确认「给你排、帮朋友、还是体验一下」。
- 演示 / 试玩场景 → 必须先声明「下面排的是虚拟样本」。

### 话术改动

- 收尾追问入口删除「对比两个人的能量互补」（合盘话术，但全库无合盘逻辑且天然踩「合不合」红线）→ 改为「帮朋友/家人也排一份」（中性且可实现）。
- PRODUCT.md 同步改写。

### 文档

- 新增 `CHANGELOG.md`（补全 skill 四件套）。
- HANDOVER.md 「已知短板」更新：「对象合盘空头支票」→「已撤掉」。

### 未解决（这次有意不做，等下一次确定优先级）

- 流年详解模板化（HANDOVER 已自认；最多人问的流年只在 JSON 不展示）
- 追问工作流（4 个入口给什么格式响应）
- `@media print`（存 PDF 排版崩）
- `examples/` 空目录、无标准产出样板
- 城市表仍只覆盖 12 座；根治需换带经纬度的城市库
- `references/五行落生活对照.md` 仅 37 行，是 SKILL 自称「最多人关注」却最薄的一块
- 缺面对「这不就是巴纳姆效应吗」的质疑话术

---

## v1.0 · 2026-09-02

初版。从压缩包安装到 user-level skills 目录。完整四件套：`SKILL.md` / `PRODUCT.md` / `HANDOVER.md` / `references/`。

安装时已知问题（修复前）：
- 城市表只有 12 座、未命中静默错盘
- 无夏令时处理
- 经度 fallback 与文档打架
- 「劫财」十神永远不出现
- HANDOVER 命令路径不一致
- 渲染 HTML 含完整个人隐私信息，无脱敏开关
- 误触发（用户问怎么用时自动排盘）

→ 以上 v1.1 全修复。
